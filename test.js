// Unused variable (esta regla generalmente está habilitada por defecto)
const unusedVariable = 42;

// Missing semicolon (si tienes habilitada la regla de semicolon)
console.log('Hello, World!');

// Incorrect indentation (si tienes habilitada la regla de indentación)
function greet() {
	console.log('Hello, World!');
}

// Using `var` instead of `let` or `const` (si tienes habilitada la regla no-var)
var oldStyleVar = 'This should be let or const';

const greeting = 'Hello, World!';
console.log(greeting);
